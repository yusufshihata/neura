I need to finish matrix multiplication now.
-----------------------------------------------------

Oberservation
-------------
when broadcasting in pytorch we only broadcast the dims that will not be involved in the matrix multplication part of ti 
we can only broadcast if some of the lesser tensor has only 1 in this dim
[1 , 2, 4] @ [3, 4, 2] -> works as we broadcast the first dim by cloning the [2,4] part of it over and over for three times
[2, 2, 4] @ [3, 4, 2] -> don't work as we can't broadcast.

Broadcasting shit how it works
------------------------------
so we will get all the dims of the tensor except for the final two dimensions
we check if they are equal then we don't need broadcasting actually we can just multiply them together
if not we need to check if some of them is 1 then we can just copy and paste the tensor in it with shape-the dim we broadcasting
if not 1 we can just return an error

Matrix multiplication
----------------------
Now we can do the matrix multiplication on the two dimensions left and then return the resulting matrix so we can save it afterward.

SO how we do this in code bitch.
---------------------------------
1. We need a braodcasting function

function broadcast(a: tensor, b: tensor) -> vec<usize> {
    a_shape_vec, b_shape_vec = a.shape, b.shape

    broadcasting_dims_a, broadcasting_dims_b = a.a_shape_vec[:2], b.b_shape_vec[:2]

    if broadcasting_dims_a == broadcasting_dims_b: do nothing

    else 
        if broadcasting_shape_a != 1 and broadcasting_shape_b != 1
            return error
        for i, dim in enumerate(broadcasting_dims_a)
            dim = max(dim, broadcasting_dims_b[i])
    return broadcasting_shape_a
}

2. We need to do matrix multiplication

function matmul(a: tensor, b: tensor) -> tensor {}
